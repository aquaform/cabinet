export type ResourceType = "book" | "file" | "res" | "recording";

export const resourceTypes = {
    book: "book" as ResourceType,
    file: "file" as ResourceType,
    res: "res" as ResourceType,
    recording: "recording" as ResourceType,
};
